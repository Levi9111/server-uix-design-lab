export type TReview = {
  name: string;
  role: string;
  company: string;
  testimonial: string;
  roi?: string;
  revenue?: string;
  avatarUrl?: string;
  color?: string;
  stats?: Record<string, string>;
};
