# AGENTS.md

> Auto-generated by [create-express-modular](https://create-express-modular.lovable.app/). This file helps AI coding assistants understand the project.

## Project: server-uix-design-lab

A production-ready, domain-driven **Express + TypeScript** backend.

## Tech Stack

- **Runtime**: Node.js ≥ 18
- **Framework**: Express
- **Language**: TypeScript (strict)
- **Database / ORM**: Mongoose (MongoDB)
- **Validation**: Zod
- **Package Manager**: npm
- **Auth**: JWT with bcrypt + rate limiting (HTTP-only cookies)
- **Docker**: Dockerfile + docker-compose

## Architecture Rules

1. All source code lives under `src/`.
2. **Modules** follow domain-driven design: `src/app/modules/<Name>/` with files named `<name>.controller.ts`, `<name>.service.ts`, `<name>.route.ts`, `<name>.model.ts`, `<name>.interface.ts`, `<name>.validation.ts`.
3. **Middleware** files MUST be named `<name>.middleware.ts` inside `src/app/middlewares/`. The build will fail otherwise.
4. Routes are auto-registered in `src/app/routes/index.ts` via inject markers (`// --- INJECT IMPORTS HERE ---` and `// --- INJECT ROUTES HERE ---`). Do NOT remove these markers.
5. **All environment variables** go through `src/app/config/index.ts` — never use `process.env` directly in application code.
6. Wrap all async route handlers with `catchAsync()` from `src/app/utils/catchAsync.ts`.
7. Use `sendResponse()` from `src/app/utils/sendResponse.ts` for all API responses.
8. Use `validateRequest()` middleware to validate incoming request bodies against schemas.
9. Use `QueryBuilder` from `src/app/utils/QueryBuilder.ts` for search, filter, sort, and pagination on Mongoose queries.

## Model Pattern

Mongoose schemas — `new Schema<IModel>({...})` exported as `model<IModel>('Name', schema)`

## CLI Commands

Use the CEM CLI for scaffolding — do NOT create module/middleware files manually.

| Command | Effect |
|---|---|
| `cem add module <Name...>` | Scaffold module(s) with all files + auto-wire routes |
| `cem add middleware <name...>` | Create middleware file(s) with correct naming |
| `cem add env <KEY...>` | Add env var(s) to `.env`, `.env.example`, and `config/index.ts` |
| `cem remove module <Name...>` | Delete module(s) + unwire route(s) |
| `cem remove middleware <name...>` | Delete middleware file(s) |
| `cem remove env <KEY...>` | Remove env var(s) from all config files |
| `cem dev` | Start dev server with hot reload |
| `cem build` | Convention guards + TypeScript compilation |
| `cem check` | Type-check + lint + format check |

## Do NOT

- Create module files manually — use `cem add module`.
- Use `process.env` directly — import `config` from `src/app/config`.
- Rename middleware files without the `.middleware.ts` suffix — `cem build` will reject it.
- Remove the inject markers in `src/app/routes/index.ts`.
- Modify core middlewares (`globalErrorHandler`, `notFound`, `auth`, `rateLimiter`) unless intentional.
